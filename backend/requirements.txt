# Python 依赖管理文件
# 使用 pip install -r requirements.txt 可以一键安装所有依赖

# ============= Web 框架 =============
# Flask - 轻量级 Web 框架，用于创建 API 服务器
Flask==3.0.0

# Flask-CORS - 处理跨域请求，让前端页面可以访问这个 API
flask-cors==4.0.0

# WSGI 服务器 - Render 生产环境部署
gunicorn==21.2.0

# ============= HTTP 请求 =============
# requests - HTTP 请求库，用于调用 Dify API
requests==2.31.0

# ============= 环境变量管理 =============
# python-dotenv - 从 .env 文件加载环境变量
python-dotenv==1.0.0

# ============= 数据库（可选） =============
# psycopg2-binary - PostgreSQL 数据库驱动（如果需要数据库功能）
# psycopg2-binary==2.9.9

# ============= 异步处理（可选） =============
# gevent - 异步网络库，提高并发性能
# gevent==23.9.1

# ============= 日志和监控（可选） =============
# Werkzeug - Flask 的底层 WSGI 工具库
Werkzeug==3.0.1

# ============= 类型提示 =============
# typing - Python 类型提示支持（Python 3.9+ 内置，无需安装）
# typing-extensions - 额外的类型提示工具
typing-extensions==4.9.0